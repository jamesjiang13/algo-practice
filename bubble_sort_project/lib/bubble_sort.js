function swap(array, idx1, idx2) {

}

function bubbleSort(array) {
    let sorted = false;
    while (!sorted) {
        sorted = true;
        for (let i = 0; i < array.length - 1; i++) {
            if (array[i] =)
            
        }
    }
}


module.exports = {
    bubbleSort,
    swap
};